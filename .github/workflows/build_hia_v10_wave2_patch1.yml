name: REJECTED — old HIA V10 Shorts semantic rebuild

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rejected:
    runs-on: ubuntu-latest
    steps:
      - name: Stop obsolete workflow
        run: |
          echo "REJECTED / DO NOT USE"
          echo "This workflow belonged to the obsolete HIA V10 Shorts semantic-rebuild system."
          echo "Active source of truth: CHANNEL/SHORTS_PRODUCTION_CANON.md"
          echo "Active model: Youtube-1 direct-cut master wrapper."
          exit 1
