# SATSOP Remotion V1

Stage 8 editorial rough cut. It assembles the locked 109-beat visual map against the locked voice master.

- 1920x1080, 30 fps
- word-timed English captions, active word `#F28A3A`
- generated images identified as `AI RECONSTRUCTION`
- official-record beats rendered as sourced document cards
- restrained one-direction reframing; no oscillating zoom

Run `npm.cmd run build-assets`, then `npm.cmd run render`.
